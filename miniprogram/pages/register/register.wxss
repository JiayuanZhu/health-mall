.register-page {
  min-height: 100vh;
  background: linear-gradient(180deg, #ecfdf5 0%, #f5f5f5 50%);
}

/* Logo区域 */
.logo-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding-top: 80rpx;
  padding-bottom: 40rpx;
}

.logo-icon {
  width: 120rpx;
  height: 120rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #ffffff;
  border-radius: 30rpx;
  margin-bottom: 20rpx;
  font-size: 60rpx;
  box-shadow: 0 8rpx 32rpx rgba(16, 185, 129, 0.15);
}

.logo-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #1a1a1a;
  margin-bottom: 8rpx;
}

.logo-subtitle {
  font-size: 26rpx;
  color: #6b7280;
}

/* 表单 */
.form-section {
  padding: 0 40rpx;
}

.form-card {
  background: #ffffff;
  border-radius: 24rpx;
  padding: 40rpx 36rpx;
  box-shadow: 0 4rpx 24rpx rgba(0, 0, 0, 0.06);
}

.form-group {
  margin-bottom: 28rpx;
}

.input-wrap {
  display: flex;
  align-items: center;
  background: #f8f9fa;
  border: 2rpx solid #e5e7eb;
  border-radius: 16rpx;
  padding: 8rpx 24rpx;
}

.input-icon {
  font-size: 32rpx;
  margin-right: 16rpx;
}

.form-input {
  flex: 1;
  font-size: 28rpx;
  padding: 16rpx 0;
}

.register-btn {
  background: linear-gradient(135deg, #10b981, #34d399);
  color: #ffffff;
  text-align: center;
  padding: 26rpx;
  border-radius: 44rpx;
  font-size: 32rpx;
  font-weight: 600;
  margin-top: 12rpx;
  box-shadow: 0 4rpx 20rpx rgba(16, 185, 129, 0.3);
}

.register-btn.disabled {
  opacity: 0.6;
}

.login-link {
  text-align: center;
  margin-top: 32rpx;
  font-size: 26rpx;
  color: #6b7280;
}

.link-text {
  color: #10b981;
  margin-left: 8rpx;
  font-weight: 500;
}
